{% extends 'base.html.twig' %}

{% block title %}NoteFrais index{% endblock %}

{% block body %}
    <h1>NoteFrais index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Client</th>
                <th>Categorie</th>
                <th>PrixHt</th>
                <th>Quantite</th>
                <th>Km</th>
                <th>PrixKm</th>
                <th>TotalTTC</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for devi in NoteFrais %}
            <tr>
                <td>{{ devi.id }}</td>
                <td>{{ devi.titre }}</td>
                <td>{{devi.client.name}}</td>
                <td>{{ devi.categorie }}</td>
                <td>{{ devi.prixHt }}</td>
                <td>{{ devi.quantite }}</td>
                <td>{{ devi.km }}</td>
                <td>{{ devi.prixKm }}</td>
                <td>{{ devi.totalTTC }}</td>
                <td>
                    <a href="{{ path('app_NoteFrais_show', {'id': devi.id}) }}">show</a>
                    <a href="{{ path('app_NoteFrais_edit', {'id': devi.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_NoteFrais_new') }}">Create new</a>
    <a href="{{ path('app_NoteFrais_export_excel') }}" class="btn btn-primary">Export to Excel</a>
{% endblock %}
